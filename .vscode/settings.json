{
    "files.associations": {
        "iomanip": "cpp"
    },

    "catch2TestExplorer.executables": [
        {
            "name": "${filename}",
            "description": "${relDirpath}/",
            "pattern": "build/bin/*test*"
        }
    ]
}